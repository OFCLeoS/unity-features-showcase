using UnityEngine;

[System.Obsolete("MISSING NPC LOGIC")]
public class AIHearing : AIModule
{
    // [SerializeField] NPC npc;

    // #region Initialization
    // void Awake()
    // {
    //     if (!npc) npc = transform.root.GetComponent<NPC>();
    // }
    // #endregion

    // public void HearNoise(SoundType noiseType, Vector3 noiseSourcePosition) => npc.HearNoise(noiseType, noiseSourcePosition);
}
